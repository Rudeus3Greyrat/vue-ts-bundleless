import type { AppRouteModule } from '/@/router/types';

const demo: AppRouteModule = {
    path: '',
    name: 'demo',
    component: {},
    redirect: '',
    meta: {
        title: '',
    },
    children: []
};

export default demo;
